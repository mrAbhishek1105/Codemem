const fs = require("fs");
const path = "D:\\Mine\\codemem\\.codemem\\db\\index.json";
const bak = path + ".bak";
if (!fs.existsSync(bak)) fs.writeFileSync(bak, fs.readFileSync(path, "utf8"));
const data = fs.readFileSync(path, "utf8");
const idx = data.lastIndexOf("}]}");
console.log("idx", idx, "len", data.length);
console.log("slice", JSON.stringify(data.slice(idx-10, idx+10)));
if (idx === -1) throw new Error("no end marker");
fs.writeFileSync(path, data.slice(0, idx+3));
console.log("repaired");
